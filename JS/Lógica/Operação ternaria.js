// (condicional) ? 'valor para verdadeiro' : 'valor para falso';
const pontuacao = 500;
const nivel = pontuacao >= 1000 ? 'Usuário Vip' : 'Usuário normal';
console.log(nivel);

const cor = null
const padrao = cor || 'PRETA'

console.log(nivel, padrao)
